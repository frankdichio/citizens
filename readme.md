App for test running on docker

Images for php, mysql, nginx

Run the usual commands to start the app (composer install, cp env, key generate..)

On the first migration run --seed to populate the role table